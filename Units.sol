//import qwer
pragma solidity ^0.4.0;

contract Simplen{
    // ether szabo finney wei
    bool isEqual = (1 ether == 5000 finney);

    // time units
    // seconds, minutes, hours, days, weeks
    bool isTrue = (1 == 1 seconds);
    bool isTrue = (1 hours == 60 minutes);

    
}